using System;

public class SAMEBit
{
    public int frequency;
    public float length;
    public SAMEBit(int Frequency, float Length)
    {
        frequency = Frequency;
        length = Length;
    }
}
